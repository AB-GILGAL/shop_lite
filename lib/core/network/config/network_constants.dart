/// Common network-related constant values.
abstract final class NetworkConstants {
  const NetworkConstants._();

  /// Default media type for JSON requests.
  static const String jsonContentType = 'application/json';

  /// Default media type expected in responses.
  static const String jsonAccept = 'application/json';
}